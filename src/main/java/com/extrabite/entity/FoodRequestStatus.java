package com.extrabite.entity;

// Food request ka status batata hai
public enum FoodRequestStatus {
    OPEN,
    OFFERED,
    ACCEPTED,
    COMPLETED,
    CANCELLED,
    AWAITING_PICKUP,
    CLOSED
}